#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, char** argv)
{
	int opcao1;
    char opcao2;
	int n1, n2, n3;
	printf("1: Pergunta e resposta\n ");
	printf("2: Cobra na caixa\n");
	printf("3: Gousmas war\n");
	printf("4: Sair\n");
	scanf("%d",&opcao1);

	if(opcao1 == 1)
	{
		do{
			printf("quanto eh 1 + 1\n");
			printf("a) 1\n");
			printf("b) 2\n");
			printf("c) 7\n");
			scanf(" %c",&opcao2);
			if(opcao2 == 'b')
			{
			printf("Voce acertou!!!!!\n");
			}
			else if(opcao2 == 'a' || opcao2 == 'c')
			{
				printf("Voce errou ;-;\n");
			}
			else
			{
				printf("escolha uma da alternativas mostradas\n");	
			}
		
		printf("Voce deseja recomecar?  1 (sim) e 0 (nao)");
		scanf("%d",&n1);
			
			
		}
		while(n1 == 1);
	
	int p1, p2;		
    
	}
    else if(opcao1 == 2)
    	do{
		printf("Escolha os seus personagens:\n ");
		printf("1)Jack Sparrow\n");
		printf("Desc.: O maior pirata dos sete mares agora vai em busca de mais um tesouro para a sua colecao\n\n");
		
		printf("2)Kiko\n");
		printf("Desc.: Detentor da bola quadrada, filho de marinheiro e o tesouro da dona Florinda esse sujeito tenta mais uma vez ser o melhor, apos descobrir que o chaves conseguiu uma moeda de prata\n\n");
		
        printf("3)Indiana Jones\n");
		printf("Desc.: O maior cacador de tesouros e desbravador de tumbas do planeta. Sera que vai conseguir botar mais uma no seu curriculo??? \n\n");
		
		printf("4)Anrsene Lupin\n");
		printf("Desc.: O maior ladrao de Paris e dentre todas a suas facanhas hoje ele tenta abrir os seus horizontes no Egito\n\n");	
			
		printf("5)Han Solo\n");
		printf("Desc.: O maior cacador de recompensas da galaxia, tenta mais uma vez provar que e o melhor \n\n");
		
		printf("6)Rainha Elizabeth\n");
		printf("Desc.: Todos pensavam que ela nao estava mais entre nos, porem apos colecionar inumeros artefatos desde a era pre-cambriana ela retorna para ter mais um em sua colecao\n\n");
		
		printf("7)exterminador do futuro\n");
		printf("Desc.: \n\n");
		
		scanf("%d", &p1);
		scanf("%d", &p2);
		
		//texto para historia
		printf("algo\n\n");
		
		
        srand(time(NULL)); // Inicializa a semente para gerar números aleatórios diferentes a cada execução
        int numero1 = (rand() % 5) + 1; // Gera um número entre 1 e 5
        int numero2 = (rand() % 5)+ 1;
        if(numero1 == numero2)
   	    {
		    numero2 = (rand() % 5)+ 1;
  	    }
		int	a, b, c, d, e;
				
		
		printf("Voce deseja jogar de novo? 1 (Sim)  0 (nao)");
		scanf("%d", &n2);
		}
		while(n2 == 1);
		 
	
	
	return 0;
}
